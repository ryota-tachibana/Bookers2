<h2>New book</h2>
<%= form_for(book) do |f| %>
<% if book.errors.any? %>
<div class=error>
<h4><%= book.errors.count %>error</h4>
　　<ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
    </div>
<% end %>
  <h5>Title</h5>
  <%= f.text_field :title,class: "col-xs-12" %>
  <h5>Opinion</h5>
  <%= f.text_area :body,class: "col-xs-12" %>
  <nav>
  <%= f.submit 'Create Book',class: "col-xs-12 btn btn-primary" %>
  </nav>
<% end %>
</div>